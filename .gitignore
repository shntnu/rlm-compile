__pycache__/
*.pyc
.venv/
.env
.claude/worktrees/
.remember/

# Scratch artifacts from ad-hoc runs.
# Curated traces are committed as *.jsonl.gz; example POCs write plain *.jsonl.
# Committed compiled artifacts are named after the example; ad-hoc ones use the live_ prefix.
traces/*.jsonl
compiled/live_*.py
